apiVersion: v1
kind: Service
metadata:
  name: sidecar-injector-webhook
  namespace: default
  labels:
    app: sidecar-injector
spec:
  ports:
  - port: 443
    targetPort: 8443
    protocol: TCP
    name: https
  selector:
    app: sidecar-injector
  type: ClusterIP
